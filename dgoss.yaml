command:
  version:
    # required attributes
    exit-status: 0
    # defaults to hash key
    exec: "hadolint --version"
    # optional attributes
    stdout:
    - Haskell Dockerfile Linter 1.17.2
    stderr: []
    timeout: 10000 # in milliseconds
    skip: false
